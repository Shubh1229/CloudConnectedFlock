apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-service
  namespace: ccflock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-service
  template:
    metadata:
      labels:
        app: weather-service
    spec:
      containers:
        - name: weather-service
          image: asingh1229/weather-service:latest
          ports:
            - containerPort: 9002
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: ccflock
spec:
  selector:
    app: weather-service
  ports:
    - port: 9002
